//Call back hell 

// call back hell - is multiple calls - asynchronous operation

//Promise is a JS object used to handle asynchrous operation

// states - pending, resolve(), reject()

/*
let studentRecord = new Promise(resolve,rejects)=>{

}

studentRecord
.then(result=>(console.log(result)))
.catch(error=>(console.log(error)))
*/

